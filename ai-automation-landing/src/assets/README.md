# Assets

Place optimized images and fonts here. Astro will process them at build time.

- Images in `src/assets/` → auto-optimized (WebP/AVIF, resized)
- Images in `public/` → served as-is (no optimization)
